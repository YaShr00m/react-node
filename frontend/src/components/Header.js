import React, { useState } from 'react';

function Header() {
    return (
        <header>
            <nav>
                <h1>Nikita's Dashboard</h1>
            </nav>
        </header>
    );
}

export default Header;